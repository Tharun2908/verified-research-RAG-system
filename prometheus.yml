global:
  scrape_interval: 15s        # how often to scrape

scrape_configs:
  - job_name: "verified-research-agent"
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8000"]